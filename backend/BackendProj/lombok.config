# Mark Lombok-generated members with @lombok.Generated so JaCoCo ignores them
# (getters/setters/builders/equals/hashCode/toString). Without this, the
# DTO/entity coverage is unfairly penalised for code Lombok writes for us.
lombok.addLombokGeneratedAnnotation = true
config.stopBubbling = true
