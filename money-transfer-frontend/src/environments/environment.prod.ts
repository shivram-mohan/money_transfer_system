export const environment = {
    production: true,
    apiUrl: 'https://your-production-url.com/api/v1',
    // Must match the production backend's `app.crypto.secret`.
    balanceSecret: 'CHANGE-ME-to-match-prod-backend-app.crypto.secret'
  };
